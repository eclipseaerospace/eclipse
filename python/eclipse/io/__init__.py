# SPDX-License-Identifier: Apache-2.0
#
# eclipse.io — reading curated data files into typed objects.
#
# Deliberately empty of code. Shadowing the stdlib io module is safe because
# Python 3 resolves imports absolutely, so `import io` inside eclipse still finds
# the standard library.
